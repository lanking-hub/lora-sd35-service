# PyTorch (通常用 conda 或特殊源安装，这里仅作参考)
# AutoDL 环境通常已安装
# torch>=2.0.0
# torchvision>=0.15.0
# torchaudio>=2.0.0

# Diffusers 和核心依赖（版本匹配阿里云镜像源可用版本）
diffusers>=0.35.0
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.0
peft>=0.17.0

# Hugging Face Hub
huggingface-hub>=0.20.0
sentencepiece>=0.2.0

# 图像处理
Pillow>=10.0.0

# 阿里云 OSS SDK
oss2>=2.18.0

# YAML 配置文件
pyyaml>=6.0

# HTTP 请求
requests>=2.31.0

# OpenAI SDK (用于通义千问 OpenAI 兼容模式)
openai>=1.0.0

# Web 服务框架
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0

# 其他工具
numpy>=1.24.0

# 模型序列化（SD3.5 必需）
protobuf>=3.20.0

